# Orion LiveKit Token Server

This project exposes a public endpoint that returns a LiveKit access token
for a Flutter mobile application.

## 🔗 Public Endpoint
`GET /getToken`

## 🚀 Deployment
Deployed on Render (free plan).

## ⚙️ Environment variables needed
- LIVEKIT_API_KEY
- LIVEKIT_API_SECRET

## ▶️ Local development
pip install -r requirements.txt
python server.py
